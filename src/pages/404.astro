---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="404: Block Not Found: CryptoSynth.id" description="Halaman yang kamu cari tidak ditemukan. Mungkin transaksinya revert.">
  <section class="error-page" transition:animate="fade">
    <div class="container">
      <div class="error-content">
        <div class="error-icon">
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10" stroke="var(--primary)" fill="transparent" stroke-opacity="0.3"/>
            <line x1="15" y1="9" x2="9" y2="15" stroke="var(--primary)"/>
            <line x1="9" y1="9" x2="15" y2="15" stroke="var(--primary)"/>
            <path d="M12 6v2" stroke="var(--primary)"/>
            <path d="M12 12v4" stroke="var(--primary)"/>
            <path d="M10 18h4" stroke="var(--primary)"/>
          </svg>
        </div>
        <div class="error-code">404</div>
        <h1>Block Tidak Ditemukan</h1>
        <p class="error-subtitle">Transaksi kamu mungkin <strong>revert</strong>, atau halaman ini udah <strong>dihapus</strong> dari state tree.</p>

        <div class="error-details">
          <div class="error-detail-item">
            <span class="error-label">Tx Hash</span>
            <span class="error-value error-hash">0x0000...0000</span>
          </div>
          <div class="error-detail-item">
            <span class="error-label">Status</span>
            <span class="error-value error-status"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="var(--accent-error)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="icon-status"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg> Reverted</span>
          </div>
          <div class="error-detail-item">
            <span class="error-label">RPC Error</span>
            <span class="error-value error-rpc">execution reverted: page not found</span>
          </div>
        </div>

        <div class="error-actions">
          <a href="/" class="btn btn-primary">← Kembali ke Beranda</a>
          <a href="/articles" class="btn btn-outline">Lihat Semua Catatan</a>
          <a href="/search" class="btn btn-ghost"><svg class="btn-icon icon-btn" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg> Cari Artikel</a>
        </div>

        <div class="error-fun">
          <p class="error-fun-label">Atau coba action lain:</p>
          <div class="error-fun-links">
            <a href="/category/airdrop" aria-label="Halaman airdrop"><svg class="fun-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg> Claim Airdrop</a>
            <a href="/scam-check" aria-label="Cek apakah domain atau address scam"><svg class="fun-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg> Cek Scam</a>
            <a href="/category/tutorial" aria-label="Tutorial crypto"><svg class="fun-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/></svg> Tutorial</a>
          </div>
          <p class="error-console">console.log(<span class="error-string">"Gas: 404, status: reverted: mungkin kamu salah alamat."</span>);</p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style is:global>
  .error-page {
    min-height: 65vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 3rem 0;
  }

  .error-content { max-width: 520px; }

  .error-icon {
    margin-bottom: 0.75rem;
    display: inline-block;
    animation: errorFloat 3s ease-in-out infinite;
  }

  @keyframes errorFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }

  .error-code {
    font-family: var(--font-display);
    font-size: 6rem;
    font-weight: 800;
    letter-spacing: -0.08em;
    line-height: 1;
    color: var(--primary);
    margin-bottom: 0.25rem;
  }

  .error-content h1 {
    font-size: 1.6rem;
    margin-bottom: 0.5rem;
  }

  .error-subtitle {
    color: var(--text-secondary);
    font-size: 0.95rem;
    margin-bottom: 2rem;
    line-height: 1.5;
  }

  .error-subtitle strong { color: var(--text-primary); }

  /* Error Details — crypto terminal style */
  .error-details {
    background: var(--bg-surface);
    border-radius: var(--radius);
    padding: 1rem 1.25rem;
    margin-bottom: 2rem;
    text-align: left;
    font-size: 0.82rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    border: 1px solid var(--border-medium);
  }

  .error-detail-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .error-label {
    font-weight: 600;
    color: var(--text-muted);
    min-width: 72px;
    font-size: 0.88rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .error-value { color: var(--text-primary); font-family: 'JetBrains Mono', monospace; font-size: 0.82rem; }
  .error-hash { color: var(--text-muted); }
  .error-status { color: var(--accent-error); }
  .error-rpc { color: var(--text-secondary); font-style: italic; }

  .error-actions {
    display: flex;
    gap: 0.65rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }

  .btn-ghost {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.6rem 1.2rem;
    font-size: 0.85rem;
    font-weight: 600;
    border-radius: var(--radius-pill);
    background: transparent;
    color: var(--text-secondary);
    border: 1px solid var(--border);
    cursor: pointer;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-ghost:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
    border-color: var(--border);
  }

  /* Fun section */
  .error-fun { border-top: 1px solid var(--border); padding-top: 1.5rem; }

  .error-fun-label {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
  }

  .error-fun-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    justify-content: center;
    margin-bottom: 1.25rem;
  }

  .error-fun-links a {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.35rem 0.9rem;
    background: var(--bg-hover);
    border-radius: var(--radius-pill);
    font-size: 0.82rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .error-fun-links a:hover {
    background: var(--primary);
    color: var(--text-inverse);
  }

  .fun-icon { display: inline-flex; align-items: center; }

  .error-console {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.88rem;
    color: var(--text-muted);
    background: var(--bg-elevated);
    padding: 0.5rem 0.75rem;
    border-radius: var(--radius-sm);
    display: inline-block;
    border: 1px solid var(--border);
  }

  .error-string { color: var(--accent-success); }

  @media (max-width: 600px) {
    .error-code { font-size: 4.5rem; }
    .error-content h1 { font-size: 1.3rem; }
    .error-details { font-size: 0.85rem; }
    .error-label { min-width: 60px; }
  }
</style>
