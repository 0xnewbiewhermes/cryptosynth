---
import BaseLayout from '../layouts/BaseLayout.astro';
import airdropsData from '../data/airdrops.json';

const statusMap = {
  active: { text: 'Aktif', cls: 'badge-active' },
  upcoming: { text: 'Segera', cls: 'badge-upcoming' },
  ended: { text: 'Berakhir', cls: 'badge-ended' },
  confirmed: { text: 'Terkonfirmasi', cls: 'badge-confirmed' },
};
const riskMap = {
  low: { text: 'Low', cls: 'risk-low' },
  medium: { text: 'Medium', cls: 'risk-medium' },
  high: { text: 'High', cls: 'risk-high' },
};
const costMap = {
  free: { text: 'Free', cls: 'cost-free' },
  low: { text: '<$10', cls: 'cost-low' },
  medium: { text: '$10-50', cls: 'cost-medium' },
  high: { text: '>$50', cls: 'cost-high' },
};
const diffMap = {
  easy: { text: 'Easy', cls: 'diff-easy' },
  medium: { text: 'Medium', cls: 'diff-medium' },
  advanced: { text: 'Advanced', cls: 'diff-advanced' },
};
const sybilMap = {
  low: { text: 'Low', cls: 'sybil-low' },
  medium: { text: 'Medium', cls: 'sybil-medium' },
  high: { text: 'High', cls: 'sybil-high' },
};
const confMap = {
  low: { text: 'Rendah', cls: 'conf-low' },
  medium: { text: 'Sedang', cls: 'conf-medium' },
  high: { text: 'Tinggi', cls: 'conf-high' },
};

const categories = [...new Set(airdropsData.map(a => a.category))].sort();

function formatDate(dateStr) {
  if (!dateStr) return '-';
  const d = new Date(dateStr);
  return d.toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' });
}
---

<BaseLayout
  title="Tools - CryptoSynth.id"
  description="Airdrop tracker dan scam checker - tools crypto gratis untuk komunitas Indonesia."
>
  <!-- TAB NAVIGATION -->
  <div class="tools-nav">
    <div class="container">
      <div class="tools-tabs" id="toolsTabs">
        <button class="tools-tab active" data-tab="tracker"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 4px;"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg> Airdrop Tracker</button>
        <button class="tools-tab" data-tab="scam"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 4px;"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg> Scam Checker</button>
        <button class="tools-tab" data-tab="wallet"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 4px;"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M16 12h.01M12 12h.01M8 12h.01"/></svg> Wallet Analyzer</button>
      </div>
    </div>
  </div>

  <!-- TAB: AIRDROP TRACKER -->
  <div class="tools-panel active" id="panel-tracker">
    <div class="page-hero">
      <div class="container">
        <h1><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 6px;"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>Airdrop Tracker</h1>
        <p>Tracker airdrop crypto paling lengkap. Klik kartu untuk melihat detail lengkap - risiko, biaya, wallet, sybil risk, dan lainnya.</p>
      </div>
    </div>
    <div class="tracker-page container">

      <div class="tracker-filters">
        <div class="filter-group">
          <label class="filter-label">Kategori:</label>
          <div class="filter-pills" id="categoryFilters">
            <button class="filter-pill active" data-category="all">Semua</button>
            {categories.map(cat => (
              <button class="filter-pill" data-category={cat}>{cat}</button>
            ))}
          </div>
        </div>
        <div class="filter-group">
          <label class="filter-label">Cari:</label>
          <input type="text" id="trackerSearch" class="tracker-search-input" placeholder="Nama project..." />
        </div>
      </div>

      <div class="tracker-count">
        Menampilkan <strong id="visibleCount">{airdropsData.length}</strong> dari <strong>{airdropsData.length}</strong> airdrop
      </div>

      <div class="airdrop-cards" id="airdropCards">
        {airdropsData.map((a, idx) => {
          const st = statusMap[a.status] || statusMap.active;
          const rk = riskMap[a.risk] || riskMap.medium;
          const ct = costMap[a.cost] || costMap.low;
          const df = diffMap[a.difficulty] || diffMap.medium;
          const sy = sybilMap[a.sybil] || sybilMap.medium;
          const cf = confMap[a.rewardConfidence] || confMap.medium;
          return (
            <div class="airdrop-card" data-category={a.category} data-project={a.project.toLowerCase()}>
              <div class="card-summary" data-idx={idx}>
                <div class="card-left">
                  <div class="card-project">
                    <a href={a.link} target="_blank" rel="noopener noreferrer" class="card-project-link">
                      <span class="card-project-name">{a.project}</span>
                      {a.symbol && a.symbol !== 'TBD' && <span class="card-project-symbol">{a.symbol}</span>}
                    </a>
                    <span class="card-chain">{a.chain}</span>
                  </div>
                  <div class="card-badges">
                    <span class={`badge ${st.cls}`}>{st.text}</span>
                    <span class={`badge ${rk.cls}`}>Risk: {rk.text}</span>
                    <span class={`badge ${ct.cls}`}>Cost: {ct.text}</span>
                    <span class={`badge ${df.cls}`}>{df.text}</span>
                  </div>
                </div>
                <div class="card-right">
                  <span class="card-verified">✓ {formatDate(a.lastVerified)}</span>
                  <button class="card-expand-btn" aria-label="Lihat detail">
                    <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M6 8l4 4 4-4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  </button>
                </div>
              </div>
              <div class="card-detail" id={`detail-${idx}`}>
                <div class="detail-grid">
                  <div class="detail-item">
                    <span class="detail-label">Aksi yang Diperlukan</span>
                    <span class="detail-value">{a.action}</span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Deadline</span>
                    <span class="detail-value">{a.deadline}</span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Estimasi Reward</span>
                    <span class="detail-value">{a.estimated}</span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Waktu yang Dibutuhkan</span>
                    <span class="detail-value">{a.timeNeeded}</span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Wallet yang Dibutuhkan</span>
                    <span class="detail-value">{a.wallet}</span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Network</span>
                    <span class="detail-value">
                      <span class={`badge ${a.network === 'Mainnet' ? 'net-mainnet' : 'net-testnet'}`}>{a.network}</span>
                    </span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Sybil Risk</span>
                    <span class="detail-value">
                      <span class={`badge ${sy.cls}`}>Sybil: {sy.text}</span>
                    </span>
                  </div>
                  <div class="detail-item">
                    <span class="detail-label">Potensi Reward</span>
                    <span class="detail-value">
                      <span class={`badge ${cf.cls}`}>{cf.text}</span>
                    </span>
                  </div>
                  {a.announcement && (
                    <div class="detail-item detail-full">
                      <span class="detail-label">Pengumuman Resmi</span>
                      <span class="detail-value">
                        <a href={a.announcement} target="_blank" rel="noopener noreferrer" class="detail-link">Lihat Pengumuman →</a>
                      </span>
                    </div>
                  )}
                  {a.tutorial && (
                    <div class="detail-item detail-full">
                      <span class="detail-label">Tutorial CryptoSynth</span>
                      <span class="detail-value">
                        <a href={a.tutorial} class="detail-link">Baca Tutorial →</a>
                      </span>
                    </div>
                  )}
                  {a.notes && (
                    <div class="detail-item detail-full">
                      <span class="detail-label">Catatan</span>
                      <span class="detail-value detail-notes">{a.notes}</span>
                    </div>
                  )}
                </div>
                <div class="detail-actions">
                  <a href={a.link} target="_blank" rel="noopener noreferrer" class="detail-btn detail-btn-primary">
                    Kunjungi Website →
                  </a>
                </div>
              </div>
            </div>
          );
        })}
      </div>

      <div class="tracker-disclaimer">
        <strong>Disclaimer:</strong> Informasi di halaman ini bersifat informatif dan bukan saran investasi. Selalu verifikasi dari sumber resmi project sebelum berpartisipasi. Airdrop bisa berubah sewaktu-waktu tanpa pemberitahuan.
      </div>
    </div>
  </div>

  <!-- TAB: SCAM CHECKER -->
  <div class="tools-panel" id="panel-scam">
    <section class="scam-hero">
      <div class="scam-hero-bg"></div>
      <div class="container">
        <div class="scam-hero-content">
          <div class="scam-hero-badge">
            <span class="scam-hero-dot"></span>
            Keamanan Dulu, Baru Transaksi
          </div>
          <h2 class="scam-hero-title"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 6px;"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>Cek Scam Crypto</h2>
          <p class="scam-hero-desc">
            Masukkan domain atau address crypto untuk cek apakah terdaftar sebagai scam di database phishing global kami.
          </p>
          <div class="scam-form-card">
            <div class="scam-form-card-inner">
              <form class="scam-form" id="checkForm">
                <div class="scam-input-wrap">
                  <svg class="scam-input-icon" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
                  </svg>
                  <input
                    type="text"
                    id="checkInput"
                    class="scam-input"
                    placeholder="Contoh: interest-tydro.com atau 0x... atau address Solana"
                    autocomplete="off"
                    spellcheck="false"
                    required
                  />
                </div>
                <button type="submit" class="scam-btn" id="checkBtn">
                  <span id="btnText">Periksa</span>
                  <span id="btnSpinner" class="scam-spinner" style="display:none"></span>
                </button>
              </form>
              <p class="scam-form-hint">Domain, wallet address (Ethereum, BSC, Bitcoin, Solana), atau kontrak token - diperiksa dari 3 database + analisis token real-time</p>
            </div>
          </div>
          <div class="scam-stats" id="scamStats">
            <div class="scam-stat-pulse"></div>
            <span id="statsText">Memuat database...</span>
          </div>
        </div>
      </div>
    </section>

    <section class="scam-result-section" id="checkResult" style="display:none">
      <div class="container">
        <div class="scam-result-card" id="resultCard">
          <div class="scam-result-glow"></div>
          <div class="scam-result-inner">
            <div class="scam-result-icon-wrap" id="resultIconWrap">
              <span class="scam-result-icon" id="resultIcon"></span>
            </div>
            <div class="scam-result-body">
              <h2 class="scam-result-title" id="resultTitle"></h2>
              <p class="scam-result-detail" id="resultDetail"></p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="scam-sources-section">
      <div class="container">
        <div class="scam-sources">
          <h3 class="scam-sources-title">Sumber Data</h3>
          <div class="scam-sources-grid">
            <div class="scam-source-item">
              <div class="scam-source-icon">🛡️</div>
              <div class="scam-source-info">
                <strong>ScamSniffer</strong>
                <span>347rb domain + 2.530 address phishing crypto</span>
              </div>
            </div>
            <div class="scam-source-item">
              <div class="scam-source-icon">🚫</div>
              <div class="scam-source-info">
                <strong>jarelllama/Scam-Blocklist</strong>
                <span>487rb domain scam & phishing umum</span>
              </div>
            </div>
            <div class="scam-source-item">
              <div class="scam-source-icon">🔥</div>
              <div class="scam-source-info">
                <strong>phishdestroy/destroylist</strong>
                <span>149rb domain phishing aktif</span>
              </div>
            </div>
            <div class="scam-source-item">
              <div class="scam-source-icon">⚡</div>
              <div class="scam-source-info">
                <strong>Go+ Security API</strong>
                <span>Analisis token real-time - honeypot, tax, blacklist (40+ chain)</span>
              </div>
            </div>
          </div>
          <p class="scam-sources-note">Database diperbarui otomatis setiap 6 jam. Selalu verifikasi sebelum bertransaksi.</p>
        </div>
      </div>
    </section>
  </div>

  <!-- TAB: WALLET ANALYZER -->
  <div class="tools-panel" id="panel-wallet">
    <section class="wallet-hero">
      <div class="wallet-hero-bg"></div>
      <div class="container">
        <div class="wallet-hero-content">
          <div class="wallet-hero-badge">
            <span class="wallet-hero-dot"></span>
            Lihat Portfolio Wallet-mu
          </div>
          <h2 class="wallet-hero-title"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 6px;"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M16 12h.01M12 12h.01M8 12h.01"/></svg>Wallet Analyzer</h2>
          <p class="wallet-hero-desc">
            Masukkan address wallet EVM untuk lihat portfolio, token holdings, dan total value dalam USD secara real-time.
          </p>
          <div class="wallet-form-card">
            <div class="wallet-form-card-inner">
              <form class="wallet-form" id="walletForm">
                <div class="wallet-input-wrap">
                  <svg class="wallet-input-icon" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="2" y="4" width="20" height="16" rx="2"/><path d="M16 12h.01M12 12h.01M8 12h.01"/>
                  </svg>
                  <input
                    type="text"
                    id="walletInput"
                    class="wallet-input"
                    placeholder="0x... atau ens.eth"
                    autocomplete="off"
                    spellcheck="false"
                    required
                  />
                </div>
                <div class="wallet-chain-select">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;"><circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
                  <select id="walletChain" class="wallet-select">
                    <option value="eth-mainnet">Ethereum</option>
                    <option value="base-mainnet" selected>Base</option>
                    <option value="bsc-mainnet">BSC</option>
                    <option value="matic-mainnet">Polygon</option>
                    <option value="arbitrum-mainnet">Arbitrum</option>
                    <option value="optimism-mainnet">Optimism</option>
                  </select>
                </div>
                <button type="submit" class="wallet-btn" id="walletBtn">
                  <span id="walletBtnText">Analyze</span>
                  <span id="walletBtnSpinner" class="wallet-spinner" style="display:none"></span>
                </button>
              </form>
              <p class="wallet-form-hint">EVM address aja dulu. Multi-chain otomatis terdeteksi. Data via GoldRush API.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="wallet-result-section" id="walletResult" style="display:none">
      <div class="container">
        <div class="wallet-summary" id="walletSummary">
          <div class="wallet-summary-header">
            <div class="wallet-summary-icon">
              <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M16 12h.01M12 12h.01M8 12h.01"/></svg>
            </div>
            <div class="wallet-summary-info">
              <div class="wallet-summary-address" id="walletSummaryAddress"></div>
              <div class="wallet-summary-chain" id="walletSummaryChain"></div>
            </div>
          </div>
          <div class="wallet-summary-total" id="walletSummaryTotal"></div>
        </div>

        <div class="wallet-token-list" id="walletTokenList">
          <div class="wallet-token-header">
            <h3>Token Holdings</h3>
            <span class="wallet-token-count" id="walletTokenCount"></span>
          </div>
          <div class="wallet-tokens" id="walletTokens"></div>
        </div>

        <div class="wallet-actions">
          <a class="wallet-actions-link" id="walletExplorerLink" href="#" target="_blank" rel="noopener">
            Lihat di Explorer →
          </a>
        </div>
      </div>
    </section>

    <section class="wallet-error-section" id="walletError" style="display:none">
      <div class="container">
        <div class="wallet-error-card">
          <div class="wallet-error-icon">⚠️</div>
          <div class="wallet-error-body">
            <h3 id="walletErrorTitle">Error</h3>
            <p id="walletErrorDetail">Terjadi kesalahan saat memuat data.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="wallet-info-section">
      <div class="container">
        <div class="wallet-info-card">
          <h3 class="wallet-info-title">Tentang Wallet Analyzer</h3>
          <div class="wallet-info-grid">
            <div class="wallet-info-item">
              <span class="wallet-info-icon">🔗</span>
              <div class="wallet-info-text">
                <strong>Multi-Chain</strong>
                <span>Ethereum, Base, BSC, Polygon, Arbitrum, Optimism</span>
              </div>
            </div>
            <div class="wallet-info-item">
              <span class="wallet-info-icon">💰</span>
              <div class="wallet-info-text">
                <strong>Real-Time Harga</strong>
                <span>USD quotes real-time dari Covalent/GoldRush</span>
              </div>
            </div>
            <div class="wallet-info-item">
              <span class="wallet-info-icon">🛡️</span>
              <div class="wallet-info-text">
                <strong>Spam Filter</strong>
                <span>Token spam otomatis difilter, hanya token legitimate</span>
              </div>
            </div>
            <div class="wallet-info-item">
              <span class="wallet-info-icon">🔒</span>
              <div class="wallet-info-text">
                <strong>Read-Only</strong>
                <span>Kami hanya baca data on-chain, tidak ada akses wallet</span>
              </div>
            </div>
          </div>
          <p class="wallet-info-note">Data diperbarui real-time. Harga dalam USD.</p>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  /* ===== TOOLS TAB NAV ===== */
  .tools-nav {
    background: var(--bg-elevated, #fff);
    border-bottom: 1px solid var(--border, #E4E4E7);
    position: sticky;
    top: var(--nav-height, 64px);
    z-index: 50;
  }

  .tools-tabs {
    display: flex;
    gap: 0;
  }

  .tools-tab {
    flex: 1;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
    padding: 0.85rem 1rem;
    border: none;
    background: transparent;
    color: var(--text-muted, #A1A1AA);
    font-size: 0.95rem;
    font-weight: 600;
    cursor: pointer;
    font-family: 'Inter', sans-serif;
    border-bottom: 2px solid transparent;
    transition: all 0.2s ease;
    position: relative;
  }

  .tools-tab:hover {
    color: var(--text-secondary, #52525B);
    background: var(--bg-surface, #F4F4F5);
  }

  .tools-tab.active {
    color: var(--primary, #4F46E5);
    border-bottom-color: var(--primary, #4F46E5);
  }

  .tools-panel {
    display: none;
  }

  .tools-panel.active {
    display: block;
  }

  /* ===== SCAM CHECKER inherited styles ===== */
  .scam-hero {
    position: relative;
    padding: 2.5rem 0 1.5rem;
    overflow: hidden;
    background: var(--text-primary, #1A1A2E);
  }

  .scam-hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 40%, rgba(99, 102, 241, 0.12), transparent 60%),
      radial-gradient(ellipse at 80% 60%, rgba(79, 70, 229, 0.08), transparent 50%);
    pointer-events: none;
  }

  .scam-hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 680px;
    margin: 0 auto;
  }

  .scam-hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.35rem 1rem;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 100px;
    font-size: 0.78rem;
    color: rgba(255, 255, 255, 0.75);
    margin-bottom: 1.25rem;
    backdrop-filter: blur(10px);
    font-weight: 500;
  }

  .scam-hero-dot {
    width: 6px;
    height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: scam-pulse 2s ease-in-out infinite;
  }

  @keyframes scam-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.8); }
  }

  .scam-hero-title {
    font-size: 2.2rem;
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 1.1;
    color: #fff;
    margin-bottom: 0.5rem;
  }

  .scam-hero-desc {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.65);
    max-width: 480px;
    margin: 0 auto 1.5rem;
    line-height: 1.6;
  }

  .scam-form-card {
    max-width: 580px;
    margin: 0 auto;
  }

  .scam-form-card-inner {
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg, 16px);
    padding: 1.25rem 1.5rem;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }

  .scam-form-card-inner:focus-within {
    border-color: rgba(26, 93, 227, 0.4);
    box-shadow: 0 0 30px rgba(26, 93, 227, 0.08);
  }

  .scam-form {
    display: flex;
    gap: 0.75rem;
  }

  .scam-input-wrap {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    background: rgba(0, 0, 0, 0.25);
    border: 1.5px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius, 12px);
    padding: 0 0.9rem;
    transition: border-color 0.25s ease;
  }

  .scam-input-wrap:focus-within {
    border-color: var(--primary-light, #3A7FEE);
  }

  .scam-input-icon {
    color: rgba(255, 255, 255, 0.35);
    flex-shrink: 0;
    transition: color 0.25s;
  }

  .scam-input-wrap:focus-within .scam-input-icon {
    color: var(--primary-light, #3A7FEE);
  }

  .scam-input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    padding: 0.85rem 0;
    color: #fff;
    font-size: 0.95rem;
    font-family: 'JetBrains Mono', 'SF Mono', monospace;
    font-weight: 500;
  }

  .scam-input::placeholder {
    color: rgba(255, 255, 255, 0.3);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-weight: 400;
    font-size: 0.9rem;
  }

  .scam-btn {
    padding: 0.85rem 1.75rem;
    border: none;
    border-radius: var(--radius, 12px);
    background: var(--primary);
    color: #fff;
    font-size: 0.95rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.25s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    white-space: nowrap;
    font-family: 'Inter', sans-serif;
    letter-spacing: 0.01em;
    box-shadow: 0 4px 14px rgba(26, 93, 227, 0.25);
  }

  .scam-btn:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(26, 93, 227, 0.35); }
  .scam-btn:active { transform: translateY(0); }
  .scam-btn:disabled { opacity: 0.6; cursor: not-allowed; transform: none; box-shadow: none; }

  .scam-spinner {
    width: 18px; height: 18px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: #fff;
    border-radius: 50%;
    animation: scam-spin 0.6s linear infinite;
  }

  @keyframes scam-spin { to { transform: rotate(360deg); } }

  .scam-form-hint {
    text-align: left;
    color: rgba(255, 255, 255, 0.3);
    font-size: 0.78rem;
    margin-top: 0.6rem;
    padding-left: 0.25rem;
  }

  .scam-stats {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1rem;
    color: rgba(255, 255, 255, 0.4);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .scam-stat-pulse {
    width: 6px; height: 6px;
    background: var(--accent, #4F46E5);
    border-radius: 50%;
    animation: scam-pulse 2s ease-in-out infinite;
  }

  .scam-result-section { padding: 2.5rem 0 1rem; }

  .scam-result-card {
    position: relative;
    max-width: 580px;
    margin: 0 auto;
    overflow: hidden;
    border-radius: var(--radius-lg, 16px);
    animation: scam-result-enter 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes scam-result-enter {
    from { opacity: 0; transform: translateY(12px) scale(0.97); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  .scam-result-glow {
    position: absolute; inset: 0;
    opacity: 0.12; border-radius: inherit;
    transition: background 0.4s ease;
    pointer-events: none;
  }

  .scam-result-inner {
    position: relative;
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.5rem 1.75rem;
    border: 1.5px solid;
    border-radius: inherit;
    background: var(--bg-elevated, #fff);
    transition: border-color 0.3s ease, background 0.3s ease;
  }

  .scam-result-icon-wrap {
    flex-shrink: 0;
    width: 52px; height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    font-size: 1.6rem;
  }

  .scam-result-body h2 { margin: 0 0 0.35rem; font-size: 1.15rem; font-weight: 700; }
  .scam-result-body p { margin: 0; font-size: 0.88rem; line-height: 1.55; color: var(--text-secondary, #52525B); }
  .scam-result-body strong { color: var(--text-primary, #18181B); font-weight: 600; }
  .scam-result-body small { color: var(--text-muted, #A1A1AA); font-size: 0.82rem; }

  .src-badges { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.5rem; }

  .src-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.2rem 0.6rem;
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 600;
    background: rgba(239, 68, 68, 0.08);
    border: 1px solid rgba(239, 68, 68, 0.2);
    color: #EF4444;
  }

  .flag-badge {
    display: inline-flex;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    font-size: 0.7rem;
    font-weight: 700;
    font-family: 'JetBrains Mono', monospace;
    text-transform: uppercase;
  }

  .flag-bad {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.25);
    color: #EF4444;
  }

  .scam-result-aman .scam-result-glow { background: #10B981; }
  .scam-result-aman .scam-result-inner { border-color: rgba(16, 185, 129, 0.3); background: linear-gradient(135deg, rgba(16, 185, 129, 0.04), rgba(16, 185, 129, 0.01)); }
  .scam-result-aman .scam-result-icon-wrap { background: rgba(16, 185, 129, 0.12); }
  .scam-result-aman h2 { color: #10B981; }

  .scam-result-scam .scam-result-glow { background: #EF4444; }
  .scam-result-scam .scam-result-inner { border-color: rgba(239, 68, 68, 0.3); background: linear-gradient(135deg, rgba(239, 68, 68, 0.04), rgba(239, 68, 68, 0.01)); }
  .scam-result-scam .scam-result-icon-wrap { background: rgba(239, 68, 68, 0.12); }
  .scam-result-scam h2 { color: #EF4444; }

  .scam-result-warning .scam-result-glow { background: #F59E0B; }
  .scam-result-warning .scam-result-inner { border-color: rgba(245, 158, 11, 0.3); background: linear-gradient(135deg, rgba(245, 158, 11, 0.04), rgba(245, 158, 11, 0.01)); }
  .scam-result-warning .scam-result-icon-wrap { background: rgba(245, 158, 11, 0.12); }
  .scam-result-warning h2 { color: #F59E0B; }

  .scam-sources-section { padding: 2rem 0; }

  .scam-sources {
    max-width: 580px;
    margin: 0 auto;
    padding: 1.5rem;
    background: var(--bg-surface, #F4F4F5);
    border: 1px solid var(--border, #E4E4E7);
    border-radius: var(--radius-lg, 16px);
  }

  .scam-sources-title { font-size: 0.95rem; margin-bottom: 1rem; color: var(--text-secondary, #52525B); font-weight: 600; }
  .scam-sources-grid { display: flex; flex-direction: column; gap: 0.75rem; }

  .scam-source-item {
    display: flex; align-items: center; gap: 0.75rem;
    padding: 0.6rem 0.9rem;
    background: var(--bg-elevated, #fff);
    border: 1px solid var(--border, #E4E4E7);
    border-radius: var(--radius, 12px);
    transition: border-color 0.2s, transform 0.2s;
  }

  .scam-source-item:hover { border-color: var(--primary-light, #3A7FEE); transform: translateX(3px); }
  .scam-source-icon { font-size: 1.2rem; flex-shrink: 0; }
  .scam-source-info { display: flex; flex-direction: column; gap: 0.1rem; }
  .scam-source-info strong { font-size: 0.85rem; color: var(--text-primary, #18181B); }
  .scam-source-info span { font-size: 0.78rem; color: var(--text-muted, #A1A1AA); }
  .scam-sources-note { margin-top: 1rem; font-size: 0.78rem; color: var(--text-muted, #A1A1AA); font-style: italic; text-align: center; }

  @media (max-width: 640px) {
    .scam-hero { padding: 2rem 0 2rem; }
    .scam-hero-title { font-size: 1.8rem; }
    .scam-hero-desc { font-size: 0.9rem; padding: 0 0.5rem; }
    .scam-form-card-inner { padding: 1rem; }
    .scam-form { flex-direction: column; }
    .scam-btn { justify-content: center; padding: 0.8rem; }
    .scam-result-inner { flex-direction: column; text-align: center; padding: 1.25rem; }
    .scam-sources { margin: 0 1rem; }
  }

  /* ===== WALLET ANALYZER ===== */
  .wallet-hero {
    position: relative;
    padding: 2.5rem 0 1.5rem;
    overflow: hidden;
    background: linear-gradient(135deg, #0F0F1A 0%, #1A1A2E 50%, #0F0F1A 100%);
  }

  .wallet-hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 15% 50%, rgba(99, 102, 241, 0.15), transparent 60%),
      radial-gradient(ellipse at 85% 30%, rgba(79, 70, 229, 0.08), transparent 50%);
    pointer-events: none;
  }

  .wallet-hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 720px;
    margin: 0 auto;
  }

  .wallet-hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.35rem 1rem;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 100px;
    font-size: 0.78rem;
    color: rgba(255, 255, 255, 0.75);
    margin-bottom: 1.25rem;
    backdrop-filter: blur(10px);
    font-weight: 500;
  }

  .wallet-hero-dot {
    width: 6px;
    height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: wallet-pulse 2s ease-in-out infinite;
  }

  @keyframes wallet-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.8); }
  }

  .wallet-hero-title {
    font-size: 2.2rem;
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 1.1;
    color: #fff;
    margin-bottom: 0.5rem;
  }

  .wallet-hero-desc {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.65);
    max-width: 500px;
    margin: 0 auto 1.5rem;
    line-height: 1.6;
  }

  .wallet-form-card { max-width: 680px; margin: 0 auto; }

  .wallet-form-card-inner {
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg, 16px);
    padding: 1.25rem 1.5rem;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }

  .wallet-form-card-inner:focus-within {
    border-color: rgba(99, 102, 241, 0.4);
    box-shadow: 0 0 30px rgba(99, 102, 241, 0.08);
  }

  .wallet-form {
    display: flex;
    gap: 0.6rem;
    align-items: stretch;
  }

  .wallet-input-wrap {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    background: rgba(0, 0, 0, 0.25);
    border: 1.5px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius, 12px);
    padding: 0 0.9rem;
    transition: border-color 0.25s ease;
  }

  .wallet-input-wrap:focus-within {
    border-color: var(--primary-light, #6366F1);
  }

  .wallet-input-icon {
    color: rgba(255, 255, 255, 0.35);
    flex-shrink: 0;
    transition: color 0.25s;
  }

  .wallet-input-wrap:focus-within .wallet-input-icon {
    color: var(--primary-light, #6366F1);
  }

  .wallet-input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    padding: 0.85rem 0;
    color: #fff;
    font-size: 0.95rem;
    font-family: 'JetBrains Mono', 'SF Mono', monospace;
    font-weight: 500;
  }

  .wallet-input::placeholder {
    color: rgba(255, 255, 255, 0.3);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-weight: 400;
    font-size: 0.9rem;
  }

  .wallet-chain-select {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    background: rgba(0, 0, 0, 0.25);
    border: 1.5px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius, 12px);
    padding: 0 0.8rem;
    color: rgba(255, 255, 255, 0.5);
    transition: border-color 0.25s;
  }

  .wallet-chain-select:focus-within {
    border-color: var(--primary-light, #6366F1);
  }

  .wallet-select {
    background: transparent;
    border: none;
    outline: none;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.85rem;
    font-weight: 600;
    font-family: 'Inter', sans-serif;
    padding: 0.7rem 0;
    cursor: pointer;
    min-width: 80px;
  }

  .wallet-select option {
    background: #1A1A2E;
    color: #fff;
  }

  .wallet-btn {
    padding: 0.85rem 1.5rem;
    border: none;
    border-radius: var(--radius, 12px);
    background: linear-gradient(135deg, var(--primary), var(--primary-light));
    color: #fff;
    font-size: 0.9rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.25s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    white-space: nowrap;
    font-family: 'Inter', sans-serif;
    letter-spacing: 0.01em;
    box-shadow: 0 4px 14px rgba(99, 102, 241, 0.25);
  }

  .wallet-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 20px rgba(99, 102, 241, 0.35);
  }

  .wallet-btn:active { transform: translateY(0); }
  .wallet-btn:disabled { opacity: 0.6; cursor: not-allowed; transform: none; box-shadow: none; }

  .wallet-spinner {
    width: 18px; height: 18px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: #fff;
    border-radius: 50%;
    animation: wallet-spin 0.6s linear infinite;
  }

  @keyframes wallet-spin { to { transform: rotate(360deg); } }

  .wallet-form-hint {
    text-align: left;
    color: rgba(255, 255, 255, 0.3);
    font-size: 0.78rem;
    margin-top: 0.6rem;
    padding-left: 0.25rem;
  }

  /* ===== RESULT ===== */
  .wallet-result-section { padding: 2.5rem 0 1.5rem; }

  .wallet-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.5rem 1.75rem;
    background: var(--bg-elevated, #fff);
    border: 1.5px solid var(--border, #E4E4E7);
    border-radius: var(--radius-lg, 16px);
    animation: wallet-enter 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes wallet-enter {
    from { opacity: 0; transform: translateY(12px) scale(0.97); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  .wallet-summary-header {
    display: flex;
    align-items: center;
    gap: 0.85rem;
  }

  .wallet-summary-icon {
    width: 44px; height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--primary-glow, rgba(79, 70, 229, 0.08));
    border-radius: 12px;
    color: var(--primary);
    flex-shrink: 0;
  }

  .wallet-summary-address {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
    word-break: break-all;
  }

  .wallet-summary-chain {
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 0.15rem;
  }

  .wallet-summary-total {
    font-size: 1.8rem;
    font-weight: 900;
    letter-spacing: -0.03em;
    color: var(--primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 50%;
  }

  /* ===== TOKEN LIST ===== */
  .wallet-token-list {
    margin-top: 1.5rem;
    background: var(--bg-elevated, #fff);
    border: 1.5px solid var(--border, #E4E4E7);
    border-radius: var(--radius-lg, 16px);
    overflow: hidden;
    animation: wallet-enter 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    animation-fill-mode: backwards;
  }

  .wallet-token-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--border, #E4E4E7);
  }

  .wallet-token-header h3 {
    font-size: 0.95rem;
    font-weight: 700;
  }

  .wallet-token-count {
    font-size: 0.82rem;
    color: var(--text-muted);
    font-weight: 500;
  }

  .wallet-tokens { display: flex; flex-direction: column; }

  .wallet-token-row {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    padding: 0.8rem 1.25rem;
    border-bottom: 1px solid var(--border-light, rgba(26, 26, 46, 0.03));
    transition: background 0.15s;
    text-decoration: none;
    color: inherit;
  }

  .wallet-token-row:last-child { border-bottom: none; }
  .wallet-token-row:hover { background: var(--bg-hover); }

  .wallet-token-row.native {
    background: var(--primary-glow, rgba(79, 70, 229, 0.04));
  }

  .wallet-token-row.native:hover {
    background: var(--primary-glow, rgba(79, 70, 229, 0.08));
  }

  .wallet-token-img {
    width: 36px; height: 36px;
    border-radius: 50%;
    flex-shrink: 0;
    background: var(--bg-surface);
    object-fit: contain;
  }

  .wallet-token-img-fallback {
    width: 36px; height: 36px;
    border-radius: 50%;
    flex-shrink: 0;
    background: var(--bg-surface);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .wallet-token-info { flex: 1; min-width: 0; }

  .wallet-token-name {
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--text-primary);
    display: flex;
    align-items: center;
    gap: 0.4rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .wallet-token-name .native-badge {
    flex-shrink: 0;
  }

  .wallet-token-symbol {
    flex-shrink: 0;
  }

  .wallet-token-name .native-badge {
    font-size: 0.6rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    padding: 0.1rem 0.4rem;
    border-radius: 4px;
    background: var(--primary);
    color: #fff;
  }

  .wallet-token-balance {
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 0.05rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .wallet-token-rate {
    color: var(--text-muted);
    opacity: 0.7;
    font-size: 0.72rem;
  }

  .wallet-token-value {
    text-align: right;
    flex-shrink: 0;
  }

  .wallet-token-usd {
    font-size: 0.88rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  .wallet-token-pct {
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-top: 0.05rem;
  }

  .wallet-token-price {
    font-size: 0.72rem;
    color: var(--text-muted);
    margin-top: 0.05rem;
  }

  /* ===== ACTIONS ===== */
  .wallet-actions {
    margin-top: 1rem;
    text-align: center;
  }

  .wallet-actions-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 1.25rem;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--primary);
    background: var(--primary-glow);
    border-radius: var(--radius, 10px);
    transition: all 0.2s;
  }

  .wallet-actions-link:hover {
    background: var(--primary);
    color: #fff;
    opacity: 1;
  }

  /* ===== ERROR ===== */
  .wallet-error-section { padding: 2.5rem 0; }

  .wallet-error-card {
    max-width: 480px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    background: var(--bg-elevated, #fff);
    border: 1.5px solid rgba(239, 68, 68, 0.2);
    border-radius: var(--radius-lg, 16px);
    animation: wallet-enter 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .wallet-error-icon { font-size: 1.5rem; flex-shrink: 0; }

  .wallet-error-body h3 {
    font-size: 0.95rem;
    font-weight: 700;
    color: #EF4444;
    margin-bottom: 0.2rem;
  }

  .wallet-error-body p {
    font-size: 0.82rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  /* ===== INFO SECTION ===== */
  .wallet-info-section { padding: 1.5rem 0 2.5rem; }

  .wallet-info-card {
    max-width: 680px;
    margin: 0 auto;
    padding: 1.5rem;
    background: var(--bg-surface, #F4F4F5);
    border: 1px solid var(--border, #E4E4E7);
    border-radius: var(--radius-lg, 16px);
  }

  .wallet-info-title {
    font-size: 0.95rem;
    margin-bottom: 1rem;
    color: var(--text-secondary);
    font-weight: 600;
  }

  .wallet-info-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }

  .wallet-info-item {
    display: flex;
    align-items: flex-start;
    gap: 0.65rem;
    padding: 0.6rem 0.8rem;
    background: var(--bg-elevated, #fff);
    border: 1px solid var(--border, #E4E4E7);
    border-radius: var(--radius, 12px);
  }

  .wallet-info-icon { font-size: 1.1rem; flex-shrink: 0; margin-top: 0.1rem; }
  .wallet-info-text { display: flex; flex-direction: column; gap: 0.05rem; }
  .wallet-info-text strong { font-size: 0.82rem; color: var(--text-primary); }
  .wallet-info-text span { font-size: 0.75rem; color: var(--text-muted); line-height: 1.4; }

  .wallet-info-note {
    margin-top: 1rem;
    font-size: 0.78rem;
    color: var(--text-muted);
    font-style: italic;
    text-align: center;
  }

  @media (max-width: 640px) {
    .wallet-hero { padding: 2rem 0 2rem; }
    .wallet-hero-title { font-size: 1.8rem; }
    .wallet-hero-desc { font-size: 0.9rem; padding: 0 0.5rem; }
    .wallet-form-card-inner { padding: 1rem; }
    .wallet-form { flex-direction: column; }
    .wallet-btn { justify-content: center; padding: 0.8rem; }
    .wallet-summary { flex-direction: column; text-align: center; padding: 1.25rem; }
    .wallet-summary-header { flex-direction: column; text-align: center; }
    .wallet-summary-total { font-size: 1.4rem; word-break: break-all; }
    .wallet-info-grid { grid-template-columns: 1fr; }
    .wallet-token-row { padding: 0.7rem 0.85rem; gap: 0.6rem; }
    .wallet-token-img { width: 30px; height: 30px; }
    .wallet-token-img-fallback { width: 30px; height: 30px; font-size: 0.7rem; }
    .wallet-token-name { font-size: 0.8rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .wallet-token-balance { font-size: 0.72rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .wallet-token-usd { font-size: 0.8rem; }
    .wallet-token-pct { font-size: 0.68rem; }
    .wallet-token-value { max-width: 80px; }
    .wallet-token-symbol { display: none; }
    .wallet-chain-select { min-width: 0; }
    .wallet-select { font-size: 0.8rem; min-width: 60px; }
  }
</style>

<script>
  // ===== TAB SWITCHING =====
  (function() {
    const tabs = document.querySelectorAll('.tools-tab');
    const panels = {
      tracker: document.getElementById('panel-tracker'),
      scam: document.getElementById('panel-scam'),
      wallet: document.getElementById('panel-wallet'),
    };

    // Restore saved tab
    const saved = localStorage.getItem('tools-active-tab') || 'tracker';

    function switchTab(tabId) {
      tabs.forEach(t => t.classList.toggle('active', t.dataset.tab === tabId));
      Object.keys(panels).forEach(id => {
        panels[id].classList.toggle('active', id === tabId);
      });
      localStorage.setItem('tools-active-tab', tabId);
    }

    tabs.forEach(tab => {
      tab.addEventListener('click', () => switchTab(tab.dataset.tab));
    });

    // Activate saved tab on load
    switchTab(saved);
  })();

  // ===== TRACKER JS =====
  (function() {
    const pills = document.querySelectorAll('.filter-pill');
    const cards = document.querySelectorAll('.airdrop-card');
    const searchInput = document.getElementById('trackerSearch');
    const countEl = document.getElementById('visibleCount');
    if (!pills.length || !cards.length) return;
    let activeCategory = 'all';

    function updateCount() {
      const visible = document.querySelectorAll('.airdrop-card:not([style*="display: none"])').length;
      if (countEl) countEl.textContent = String(visible);
    }

    function filterCards() {
      const query = (searchInput?.value || '').toLowerCase().trim();
      cards.forEach(card => {
        const el = card;
        const cat = el.dataset.category;
        const text = el.textContent?.toLowerCase() || '';
        const catMatch = activeCategory === 'all' || cat === activeCategory;
        const searchMatch = !query || text.includes(query);
        el.style.display = (catMatch && searchMatch) ? '' : 'none';
      });
      updateCount();
    }

    pills.forEach(pill => {
      pill.addEventListener('click', () => {
        pills.forEach(p => p.classList.remove('active'));
        pill.classList.add('active');
        activeCategory = pill.dataset.category || 'all';
        filterCards();
      });
    });

    searchInput?.addEventListener('input', filterCards);

    document.querySelectorAll('.card-summary').forEach(summary => {
      summary.addEventListener('click', (e) => {
        if (e.target.closest('.card-project-link')) return;
        const idx = summary.dataset.idx;
        const detail = document.getElementById('detail-' + idx);
        const card = summary.closest('.airdrop-card');
        if (detail && card) {
          const isOpen = detail.classList.contains('open');
          document.querySelectorAll('.card-detail.open').forEach(d => d.classList.remove('open'));
          document.querySelectorAll('.airdrop-card.expanded').forEach(c => c.classList.remove('expanded'));
          if (!isOpen) {
            detail.classList.add('open');
            card.classList.add('expanded');
          }
        }
      });
    });
  })();

  // ===== SCAM CHECKER JS =====
  (function() {
    const dataUrl = '/data/scam-alerts.json';
    let scamData = [];
    const form = document.getElementById('checkForm');
    const input = document.getElementById('checkInput');
    const result = document.getElementById('checkResult');
    const resultCard = document.getElementById('resultCard');
    const resultIconWrap = document.getElementById('resultIconWrap');
    const resultIcon = document.getElementById('resultIcon');
    const resultTitle = document.getElementById('resultTitle');
    const resultDetail = document.getElementById('resultDetail');
    const stats = document.getElementById('scamStats');
    const statsText = document.getElementById('statsText');
    const btnText = document.getElementById('btnText');
    const btnSpinner = document.getElementById('btnSpinner');
    const btn = document.getElementById('checkBtn');
    if (!form) return;

    const SOURCE_META = {
      'ScamSniffer': { icon: '🛡️', label: 'ScamSniffer' },
      'jarelllama': { icon: '🚫', label: 'jarelllama' },
      'phishdestroy': { icon: '🔥', label: 'phishdestroy' }
    };

    // Load data
    fetch(dataUrl + '?v=' + Date.now())
      .then(r => r.json())
      .then(data => {
        scamData = data;
        const counts = {};
        data.forEach(d => {
          d.source.split(', ').forEach(s => { counts[s] = (counts[s] || 0) + 1; });
        });
        const parts = [];
        if (counts['ScamSniffer']) parts.push(`🛡️ ${(counts['ScamSniffer']).toLocaleString('id-ID')}`);
        if (counts['jarelllama']) parts.push(`🚫 ${(counts['jarelllama']).toLocaleString('id-ID')}`);
        if (counts['phishdestroy']) parts.push(`🔥 ${(counts['phishdestroy']).toLocaleString('id-ID')}`);
        if (statsText) statsText.innerHTML = parts.join(' &bull; ');
      })
      .catch(() => {
        if (statsText) { statsText.innerHTML = 'Gagal memuat database'; }
        if (stats) stats.style.color = '#EF4444';
      });

    function isAddress(input) {
      if (/^0x[a-f0-9]{40}$/i.test(input)) return 'evm';
      if (/^(bc1|[13])[a-zA-Z0-9]{25,62}$/.test(input)) return 'btc';
      if (/^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(input) && !input.includes('.')) return 'sol';
      return false;
    }

    function normalizeInput(input) {
      let d = input.trim().toLowerCase();
      for (const prefix of ['http://', 'https://', 'www.', 'hxxp://', 'hxxps://', 'ftp://']) {
        if (d.startsWith(prefix)) d = d.slice(prefix.length);
      }
      d = d.replace(/\/+$/, '').replace(/\/$/, '');
      return d;
    }

    function checkBlacklist(input, addrType) {
      if (!input || !scamData.length) return null;
      if (addrType) {
        const match = scamData.find(d => d.type === 'address' && d.key === input);
        return match ? [match] : null;
      }
      const matches = scamData.filter(d =>
        d.type === 'domain' && (
          d.key === input ||
          d.key.endsWith('.' + input) ||
          input.includes(d.key)
        )
      );
      return matches.length > 0 ? matches : null;
    }

    const CHAIN_MAP = {
      evm: 1, bsc: 56, sol: 101, polygon: 137, avalanche: 43114,
      arbitrum: 42161, optimism: 10, base: 8453
    };

    async function checkGoPlus(address, chainId, isSolana, rawInput) {
      try {
        const solParam = isSolana ? '&solana=1' : '';
        const resp = await fetch(
          `/api/goplus?chain=${chainId}&address=${encodeURIComponent(address)}${solParam}`,
          { signal: AbortSignal.timeout(10000) }
        );
        const data = await resp.json();
        const result = data.result || {};
        // Solana preserves original case; try lookupKey, then lowercased, then raw input
        const lookupKey = isSolana ? rawInput : address.toLowerCase();
        const tokenData = result[lookupKey] || result[address.toLowerCase()] || result[address];
        if (!tokenData) return null;
        const flags = [];
        if (tokenData.is_honeypot === '1') flags.push('Honeypot');
        if (tokenData.is_blacklisted === '1') flags.push('Blacklist');
        if (tokenData.cannot_buy === '1') flags.push('Cannot Buy');
        if (tokenData.cannot_sell_all === '1') flags.push('Cannot Sell All');
        if (tokenData.trading_cooldown === '1') flags.push('Trading Cooldown');
        if (tokenData.anti_whale_modifiable === '1') flags.push('Anti-Whale Modifiable');
        if (tokenData.can_take_back_ownership === '1') flags.push('Ownership Can Be Taken');
        if (tokenData.owner_change_balance === '1') flags.push('Owner Can Change Balance');
        if (tokenData.hidden_owner === '1') flags.push('Hidden Owner');
        if (tokenData.selfdestruct === '1') flags.push('Self-Destruct');
        if (tokenData.external_call === '1') flags.push('External Call');
        if (tokenData.is_airdrop_scam === '1') flags.push('Airdrop Scam');
        if (tokenData.trust_list === '0' && tokenData.is_blacklisted !== '1') flags.push('Not on Trust List');
        const buyTax = parseFloat(tokenData.buy_tax || '0');
        const sellTax = parseFloat(tokenData.sell_tax || '0');
        if (buyTax > 10) flags.push(`Buy Tax ${buyTax}%`);
        if (sellTax > 10) flags.push(`Sell Tax ${sellTax}%`);
        return {
          name: tokenData.token_name || null,
          symbol: tokenData.token_symbol || null,
          flags,
          holders: parseInt(tokenData.holder_count || '0'),
          totalSupply: tokenData.total_supply || null,
          creator: tokenData.creator_address || null,
          owner: tokenData.owner_address || null,
          decimals: tokenData.decimals || null,
          buyTax: buyTax,
          sellTax: sellTax,
          is_risky: flags.length > 0,
          raw: tokenData
        };
      } catch { return null; }
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const raw = input.value.trim();
      if (!raw) { input.focus(); return; }

      result.style.display = 'block';
      resultCard.className = 'scam-result-card';
      resultCard.style.opacity = '0';
      btnText.style.display = 'none';
      btnSpinner.style.display = 'inline-block';
      btn.disabled = true;

      await new Promise(r => setTimeout(r, 200));
      const cleaned = normalizeInput(raw);

      if (cleaned.length < 4) {
        btnText.style.display = 'inline';
        btnSpinner.style.display = 'none';
        btn.disabled = false;
        resultCard.className = 'scam-result-card scam-result-warning';
        resultIcon.textContent = '⚠️';
        resultIconWrap.style.background = 'rgba(245, 158, 11, 0.12)';
        resultTitle.textContent = 'Input Tidak Valid';
        resultDetail.textContent = 'Masukkan domain atau address yang valid.';
        resultCard.style.opacity = '1';
        result.scrollIntoView({ behavior: 'smooth', block: 'center' });
        return;
      }

      const addrType = isAddress(cleaned);
      const matches = checkBlacklist(cleaned, addrType);
      let goPlusResult = null;
      if (addrType === 'evm' || addrType === 'sol' || addrType === 'bsc') {
        const chainId = CHAIN_MAP[addrType] || 1;
        goPlusResult = await checkGoPlus(cleaned, chainId, addrType === 'sol', raw.trim());
      }

      btnText.style.display = 'inline';
      btnSpinner.style.display = 'none';
      btn.disabled = false;

      const isScam = matches && matches.length > 0;
      const goPlusRisky = goPlusResult && goPlusResult.is_risky;

      if (isScam || goPlusRisky) {
        let html = '';
        resultCard.className = 'scam-result-card scam-result-scam';
        if (isScam) {
          const sources = [...new Set(matches.flatMap(m => m.source.split(', ')))];
          const sourceBadges = sources.map(s => {
            const meta = SOURCE_META[s] || { icon: '\uD83D\uDCCB', label: s };
            return `<span class="src-badge">${meta.icon} ${meta.label}</span>`;
          }).join(' ');
          html += `<p><strong>Terdaftar sebagai scam</strong> di database kami:</p><div class="src-badges">${sourceBadges}</div>${matches.length > 1 && !addrType ? `<small>(${matches.length} varian terkait)</small>` : ''}`;
        }
        if (goPlusRisky) {
          const flagBadges = goPlusResult.flags.map(f => `<span class="flag-badge flag-bad">${f}</span>`).join(' ');
          html += `<p style="margin-top: ${isScam ? '0.75rem' : '0'}; margin-bottom: 0;"><strong>⚠️ Go+ Security:</strong> Token ini terdeteksi berbahaya</p><div class="src-badges">${flagBadges}</div>`;
        }
        resultIcon.textContent = '⚠️';
        resultIconWrap.style.background = 'rgba(239, 68, 68, 0.12)';
        resultTitle.textContent = '⚠️ SCAM / BERBAHAYA';
        resultDetail.innerHTML = html;
      } else {
        resultCard.className = 'scam-result-card scam-result-aman';
        resultIcon.textContent = '✅';
        resultIconWrap.style.background = 'rgba(16, 185, 129, 0.12)';
        resultTitle.textContent = '✅ Aman - Tidak Terdaftar';
        let html = addrType ? 'Address ini <strong>tidak terdaftar</strong> sebagai scam di database kami.' : 'Domain ini <strong>tidak terdaftar</strong> sebagai scam di database kami.';
        if (goPlusResult) {
          const r = goPlusResult;
          const nameInfo = r.name ? `<strong>${r.name}</strong>${r.symbol ? ` ($${r.symbol})` : ''}` : '';
          const holderInfo = r.holders > 0 ? ` | ${r.holders.toLocaleString()} holders` : '';
          const supplyInfo = r.totalSupply ? ` | Supply: ${r.totalSupply}` : '';
          const taxInfo = (r.buyTax > 0 || r.sellTax > 0) ? ` | Buy Tax: ${r.buyTax}% Sell Tax: ${r.sellTax}%` : '';
          const flagCount = r.flags.length > 0 ? ` (${r.flags.length} flags)` : '';
          html += `<br><small>⚡ Go+: ${nameInfo}${holderInfo}${supplyInfo}${taxInfo}${flagCount}</small>`;
        }
        html += `<br><small>Tetap verifikasi sebelum transaksi.</small>`;
        resultDetail.innerHTML = html;
      }
      resultCard.style.opacity = '1';
      result.scrollIntoView({ behavior: 'smooth', block: 'center' });
    });

    input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') form.dispatchEvent(new Event('submit'));
    });
  })();

  // ===== WALLET ANALYZER JS =====
  (function() {
    const form = document.getElementById('walletForm');
    const input = document.getElementById('walletInput');
    const chainSelect = document.getElementById('walletChain');
    const btn = document.getElementById('walletBtn');
    const btnText = document.getElementById('walletBtnText');
    const btnSpinner = document.getElementById('walletBtnSpinner');
    const result = document.getElementById('walletResult');
    const error = document.getElementById('walletError');
    const summaryAddress = document.getElementById('walletSummaryAddress');
    const summaryChain = document.getElementById('walletSummaryChain');
    const summaryTotal = document.getElementById('walletSummaryTotal');
    const tokenList = document.getElementById('walletTokens');
    const tokenCount = document.getElementById('walletTokenCount');
    const explorerLink = document.getElementById('walletExplorerLink');
    const errorTitle = document.getElementById('walletErrorTitle');
    const errorDetail = document.getElementById('walletErrorDetail');

    if (!form) return;

    function formatBalance(balance, decimals) {
      const val = BigInt(balance);
      const divisor = BigInt(10) ** BigInt(decimals);
      const whole = val / divisor;
      const frac = val % divisor;
      const fracStr = frac.toString().padStart(decimals, '0');
      const wholeStr = whole.toLocaleString('en-US');
      const significantFrac = fracStr.replace(/0+$/, '').slice(0, 6);
      if (!significantFrac) return wholeStr;
      return wholeStr + '.' + significantFrac;
    }

    function formatUSD(val) {
      if (val >= 1_000_000_000) return '$' + (val / 1_000_000_000).toFixed(2) + 'B';
      if (val >= 1_000_000) return '$' + (val / 1_000_000).toFixed(2) + 'M';
      if (val >= 1_000) return '$' + (val.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }));
      if (val >= 1) return '$' + val.toFixed(2);
      if (val >= 0.01) return '$' + val.toFixed(4);
      if (val > 0) return '$' + val.toFixed(6);
      return '$0.00';
    }

    function shortenAddress(addr) {
      if (addr.length <= 20) return addr;
      return addr.slice(0, 8) + '...' + addr.slice(-6);
    }

    function handleError(title, detail) {
      result.style.display = 'none';
      error.style.display = 'block';
      if (errorTitle) errorTitle.textContent = title;
      if (errorDetail) errorDetail.textContent = detail;
      error.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const address = input.value.trim();
      if (!address) { input.focus(); return; }

      // Validate EVM address or ENS
      const isEVM = /^0x[a-fA-F0-9]{40}$/.test(address);
      const isENS = /^[a-zA-Z0-9-]+\.eth$/.test(address);
      if (!isEVM && !isENS) {
        handleError('Address Tidak Valid', 'Masukkan address EVM yang valid (0x...) atau ENS domain (.eth).');
        btnText.style.display = 'inline';
        btnSpinner.style.display = 'none';
        btn.disabled = false;
        return;
      }

      const chain = chainSelect.value;

      // Show loading
      result.style.display = 'none';
      error.style.display = 'none';
      btnText.style.display = 'none';
      btnSpinner.style.display = 'inline-block';
      btn.disabled = true;

      try {
        const resp = await fetch('/api/wallet?address=' + encodeURIComponent(address) + '&chain=' + encodeURIComponent(chain), {
          signal: AbortSignal.timeout(15000)
        });
        const data = await resp.json();

        if (data.error || !data.items) {
          handleError('Gagal Memuat Data', data.error || 'Wallet tidak ditemukan atau tidak ada transaksi.');
          btnText.style.display = 'inline';
          btnSpinner.style.display = 'none';
          btn.disabled = false;
          return;
        }

        // Render result
        summaryAddress.textContent = isENS ? address : shortenAddress(address);
        summaryChain.textContent = data.chain_label + ' • ' + data.items.length + ' token' + ' • ' + (data.updated_at ? new Date(data.updated_at).toLocaleTimeString('id-ID') : '');
        summaryTotal.textContent = data.pretty_total || formatUSD(data.total_value);

        explorerLink.href = data.explorer_base + '/address/' + address;
        explorerLink.textContent = 'Lihat di ' + data.chain_label + ' Explorer →';

        // Render tokens
        let html = '';
        let count = 0;
        const maxVisible = 50;
        const hasMore = data.items.length > maxVisible;
        const displayItems = data.items.slice(0, maxVisible);
        const hiddenCount = data.items.length - maxVisible;

        displayItems.forEach(function(item) {
          count++;
          const balance = formatBalance(item.balance, item.contract_decimals);
          const usdValue = item.quote || 0;
          const pct = data.total_value > 0 ? ((usdValue / data.total_value) * 100).toFixed(1) : '0.0';
          const logoUrl = item.logo_urls?.token_logo_url || '';
          const nativeCls = item.is_native_token ? ' native' : '';

          html += '<div class="wallet-token-row' + nativeCls + '">';

          // Logo
          if (logoUrl) {
            html += '<img class="wallet-token-img" src="' + logoUrl + '" alt="" loading="lazy" onerror="this.style.display=\'none\';this.nextElementSibling.style.display=\'flex\'" />';
            html += '<div class="wallet-token-img-fallback" style="display:none">' + item.contract_ticker_symbol.slice(0, 2) + '</div>';
          } else {
            html += '<div class="wallet-token-img-fallback">' + item.contract_ticker_symbol.slice(0, 2) + '</div>';
          }

          // Info
          html += '<div class="wallet-token-info">';
          html += '<div class="wallet-token-name">';
          html += item.contract_name + ' <span class="wallet-token-symbol" style="color:var(--text-muted);font-weight:400;font-size:0.78rem">' + item.contract_ticker_symbol + '</span>';
          if (item.is_native_token) html += '<span class="native-badge">Native</span>';
          html += '</div>';
          html += '<div class="wallet-token-balance">' + balance + ' ' + item.contract_ticker_symbol;
          if (item.quote_rate) html += ' <span class="wallet-token-rate">@ ' + formatUSD(item.quote_rate) + '</span>';
          html += '</div>';
          html += '</div>';

          // Value
          html += '<div class="wallet-token-value">';
          html += '<div class="wallet-token-usd">' + (item.pretty_quote || formatUSD(usdValue)) + '</div>';
          html += '<div class="wallet-token-pct">' + pct + '%</div>';
          html += '</div>';

          html += '</div>';
        });

        if (hasMore) {
          html += '<div class="wallet-token-row" style="justify-content:center;color:var(--text-muted);font-size:0.82rem;font-weight:500;border-bottom:none">';
          html += '+ ' + hiddenCount + ' token lainnya (tampil 50 teratas)';
          html += '</div>';
        }

        tokenList.innerHTML = html;
        tokenCount.textContent = data.items.length + ' token' + (data.items.length > 1 ? 's' : '') + (hasMore ? ' (menampilkan 50)' : '');

        result.style.display = 'block';
        result.scrollIntoView({ behavior: 'smooth', block: 'start' });
      } catch (err) {
        handleError('Request Gagal', 'Gagal terhubung ke server. Coba lagi nanti.');
      } finally {
        btnText.style.display = 'inline';
        btnSpinner.style.display = 'none';
        btn.disabled = false;
      }
    });

    input.addEventListener('keydown', function(e) {
      if (e.key === 'Enter') form.dispatchEvent(new Event('submit'));
    });
  })();
</script>